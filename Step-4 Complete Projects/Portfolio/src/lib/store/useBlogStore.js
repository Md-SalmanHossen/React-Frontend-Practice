import { create } from "zustand";

const useBlogStore=create((set)=>({
   blogs:null,
   getBlogsRequest: async()=>{
      try {
         let res=await 
      } catch (error) {
         
      }
   };
}))